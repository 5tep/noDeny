    ABONENT_ID;REGION_ID;ID;BEGIN_TIME;END_TIME;PARAMETER;INTERNAL_ID1;INTERNAL_ID2
    36;28;6;2009-12-02 00:00:00;2011-08-31 00:00:00;;36;36
    84;28;6;2009-12-02 00:00:00;2011-08-31 00:00:00;;84;84

# Получение текущей даты
current_date=$(date +%Y%m%d_%H%M)

# Формирование MySQL-запроса с выводом в файл
echo "
SELECT 
    u.id AS ABONENT_ID, 
    gp.id AS REGION_ID, 
    u.grp AS ID, 
    FROM_UNIXTIME(u.contract_date) AS BEGIN_TIME, 
    FROM_UNIXTIME(u.modify_time) AS END_TIME, 
    '' AS PARAMETER, 
    u.id AS INTERNAL_ID1, 
    u.id AS INTERNAL_ID2
INTO OUTFILE '/var/lib/mysql-files/ABONENT_SERVICE_$current_date.txt'
FIELDS TERMINATED BY ';' 
OPTIONALLY ENCLOSED BY '\"'
LINES TERMINATED BY '\n'
FROM 
    user_grppack gp, users u
JOIN 
    user_grp ug ON u.grp = ug.grp_id
WHERE 
    u.contract_date >= UNIX_TIMESTAMP('2009-12-02 00:00:00') 
    AND u.modify_time <= UNIX_TIMESTAMP('2011-08-31 00:00:00')
    AND gp.pack_grps like CONCAT('%,', u.grp, ',%')
    AND gr.id in (13,18);
" > /var/lib/mysql-files/query.sql

# Выполнение завроса в базе данных
mysql -u username -p%пароль% big_nodeny < /var/lib/mysql-files/query.sql

# Перенос файлов, подчищаем за собой
mv -f /var/lib/mysql-files/ABONENT_SERVICE_* /path/to/your
rm -f /var/lib/mysql-files/query.sql



Альтернативный безопасный способ — использование файла конфигурации ~/.my.cnf:
Создайте файл ~/.my.cnf с таким содержимым:
ini
Копировать код
[client]
user=username
password=yourpassword
Убедитесь, что права доступа к этому файлу ограничены:
bash
Копировать код
chmod 600 ~/.my.cnf
Теперь можете использовать команду без явного указания пароля:
bash
Копировать код
mysql -e "SELECT ... "